services:
  # Redis Cache
  - type: redis
    name: claimcheck-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList:
      - source: 0.0.0.0/0
        description: Allow all

  # Backend API (Web Service)
  - type: web
    name: claimcheck-api
    runtime: node
    plan: free
    rootDir: Backend  # <-- FIX: Capital B
    buildCommand: "npm install"
    startCommand: "node server.js"
    healthCheckPath: /api/health
    envVars:
      - fromGroup: ClaimCheck Shared Secrets

  # Backend Worker (Optional, for background jobs)
  - type: worker
    name: claimcheck-worker
    runtime: node
    plan: free
    rootDir: Backend  # <-- FIX: Capital B
    buildCommand: "npm install"
    startCommand: "node worker.js"
    envVars:
      - fromGroup: ClaimCheck Shared Secrets

  # Frontend Static Site
  - type: web
    name: claimcheck-web
    runtime: static
    rootDir: Frontend # <-- FIX: Capital F
    buildCommand: "npm install && npm run build"
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: claimcheck-api
          property: url # <-- BEST PRACTICE: Use 'url' to get the full https:// address
      - fromGroup: ClaimCheck Shared Secrets